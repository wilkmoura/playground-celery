version: "3.9"

services:
    rabbitmq:
        image: rabbitmq
        ports:
            - 5672:5672
            - 15672:15672
    worker:
        build: ./
        volumes:
            - ./:/app/
        depends_on:
            - rabbitmq
        command: celery -A tasks worker --loglevel=INFO